version: "3.8"

services:
  app:
    build: .
    environment:
      SOCKET_ENDPOINT: "http://localhost"
      SOCKET_PATH: "/mysocket"
      USER_PATH: "http://localhost/users"
    ports:
      - "1234:1234"